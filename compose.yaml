services:
  rust:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rust-container
    volumes:
      - .:/rust-docker
    working_dir: /rust-docker

    ports:
      - "2222:22"

    user: root

    # 💡 command の修正: SSHDをバックグラウンドで起動し、tail -f でコンテナの実行を維持する 💡
    # これが最も安定したコンテナ維持の方法です。
    command: sh -c "/usr/sbin/sshd -D -e & tail -f /dev/null"
